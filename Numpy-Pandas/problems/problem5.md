# Problem 5 — Join and compute metric (pandas)

Given two small DataFrames (users and transactions), join them and compute the total transaction amount per user and add a column with each user's transaction share (user_total / global_total).

Include example input and expected output.
