# Problem 1 — Normalize array

Write a function that takes a 1D NumPy array and returns a normalized array scaled to the range [0, 1].

Example:

Input: [10, 20, 30] -> Output: [0.0, 0.5, 1.0]

Edge cases: handle arrays with constant values (return zeros).
