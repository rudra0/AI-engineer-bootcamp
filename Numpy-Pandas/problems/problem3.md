# Problem 3 — Missing values and filtering (pandas)

Given a CSV-like table (name, age, score) load it into a DataFrame, fill missing `score` with the column mean, and return rows where `age` &gt;= 18 sorted by `score` descending.

Describe how you'd implement and include sample input and expected output.
