# Problem 4 — Group and aggregate (pandas)

Given a DataFrame with `user_id`, `category`, and `amount`, compute per-category total and average amounts and return a DataFrame sorted by total descending.

Provide example input and expected output.
