# Problem 2 — Moving average

Implement a function that computes the moving average of a 1D numeric NumPy array given a window size `k`.

Example: array [1,2,3,4,5], k=3 -> [nan, nan, 2.0, 3.0, 4.0] (use `numpy.nan` for positions without full window)
