# Python Folder Documentation

This folder is organized into separate subfolders for each learning example. Each subfolder contains:

- the Python code file
- a dedicated `README.md` explaining the purpose, inputs, and example usage

## Learning modules

- `atmCalculator/`
- `bmiCalculator/`
- `currencyConverter/`
- `gradeCalculator/`
- `loop/`
- `miniCalculator/`
- `pattern/`
- `primeNumber/`
- `class/`
- `bankingSystemUsingFunction/`
- `passwordGenerator/`
- `passwordStrengthChecker/`
- `functions/`
- `fileIO/`
- `exceptions/`
- `dataStructures/`

## Daily workflow

When you update or add code:

1. Keep each example inside its own folder.
2. Update the example folder's `README.md` with the learning objective.
3. Stage only the changed files.
4. Commit with a clear learning-focused message.
