# Password Generator

## Purpose

Generate a strong password based on user-provided length.

## What it teaches

- reading user input
- using `random` and `string` modules
- generating secure random characters
- validating minimum password length

## How to run

```bash
python3 passwordGenerator.py
```

## Example

- Enter `10` to generate a 10-character password
- Output includes letters, digits, and symbols
